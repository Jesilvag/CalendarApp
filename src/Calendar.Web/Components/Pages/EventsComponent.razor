@page "/events"
@using Calendar.Application.DTOs
@using Calendar.Web.Api
@using Calendar.Web.DTOS
@inject IEventsApi _events

<div class="mb-3">
    <InputDate @bind-Value="date" class="form-control" />
</div>

<button class="btn btn-primary mb-3" @onclick="() => modal!.ShowCreateAsync()">Add</button>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Start</th>
            <th>End</th>
            <th>Location</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in events)
        {
            <tr>
                <td>@item.Title</td>
                <td>@item.StartDate</td>
                <td>@item.EndDate</td>
                <td>@item.Location</td>
                <td>@item.Description</td>
                <td>
                    <button class="btn btn-danger"
                            @onclick="() => modal!.ShowEditAsync(item)">Edit</button>
                </td>
            </tr>
        }
    </tbody>
</table>

<EventModal @ref="modal" OnEventChanged="LoadEvents" />

@code {
    private EventModal? modal = default!;
    private DateOnly date = DateOnly.FromDateTime(DateTime.Today);

    private List<CalendarEventDto> events = new();

    protected override async Task OnInitializedAsync()
    {
        await LoadEvents();
        await base.OnInitializedAsync();
    }

    private async Task LoadEvents()
    {
        events = (await _events.GetByDateAsync(date)).ToList();
    }
}
